// Auto-generated from examples/. Do not edit by hand; run tools/build_examples.py.
window.OBF_EXAMPLES = {
  "python": {
    "bubble_sort": "# Bubble sort a fixed list and report the number of swaps.\ndef bubble_sort(items):\n    data = list(items)\n    swaps = 0\n    for i in range(len(data)):\n        for j in range(len(data) - 1 - i):\n            if data[j] > data[j + 1]:\n                data[j], data[j + 1] = data[j + 1], data[j]\n                swaps = swaps + 1\n    return data, swaps\n\n\nnumbers = [5, 2, 9, 1, 5, 6, 3, 8, 4, 7]\nordered, count = bubble_sort(numbers)\nprint(\"sorted\", ordered)\nprint(\"swaps\", count)\n",
    "caesar": "# Caesar cipher: encode a message, then decode it back.\ndef shift_char(ch, k):\n    if \"a\" <= ch <= \"z\":\n        base = ord(\"a\")\n        return chr((ord(ch) - base + k) % 26 + base)\n    return ch\n\n\ndef encode(message, k):\n    return \"\".join(shift_char(c, k) for c in message)\n\n\ndef decode(message, k):\n    return encode(message, 26 - k)\n\n\nplain = \"the quick brown fox\"\nsecret = encode(plain, 3)\nback = decode(secret, 3)\nprint(\"plain \", plain)\nprint(\"secret\", secret)\nprint(\"back  \", back)\nprint(\"ok\", back == plain)\n",
    "fibonacci": "# Compare an iterative and a recursive Fibonacci for the first 12 terms.\ndef fib_iter(n):\n    a = 0\n    b = 1\n    for _ in range(n):\n        a, b = b, a + b\n    return a\n\n\ndef fib_rec(n):\n    if n < 2:\n        return n\n    return fib_rec(n - 1) + fib_rec(n - 2)\n\n\nvalues_iter = [fib_iter(k) for k in range(12)]\nvalues_rec = [fib_rec(k) for k in range(12)]\nprint(\"iter\", values_iter)\nprint(\"rec \", values_rec)\nprint(\"match\", values_iter == values_rec)\n",
    "fizzbuzz": "# Classic FizzBuzz for the numbers 1 through 20.\ndef classify(n):\n    if n % 15 == 0:\n        return \"FizzBuzz\"\n    if n % 3 == 0:\n        return \"Fizz\"\n    if n % 5 == 0:\n        return \"Buzz\"\n    return str(n)\n\n\ndef main():\n    results = []\n    for n in range(1, 21):\n        results.append(classify(n))\n    print(\" \".join(results))\n\n\nmain()\n",
    "primes": "# Sieve of Eratosthenes: print the primes below 50.\ndef primes_below(limit):\n    sieve = [True] * limit\n    sieve[0] = False\n    if limit > 1:\n        sieve[1] = False\n    for i in range(2, limit):\n        if sieve[i]:\n            for j in range(i * i, limit, i):\n                sieve[j] = False\n    return [i for i in range(limit) if sieve[i]]\n\n\nfound = primes_below(50)\nprint(\"count\", len(found))\nprint(\"primes\", found)\n",
    "word_count": "# Count word frequencies in a fixed passage and show the top five.\ntext = \"\"\"the quick brown fox jumps over the lazy dog\nthe dog was not amused and the fox ran away\nover the hill and far away the fox went\"\"\"\n\n\ndef count_words(passage):\n    counts = {}\n    for word in passage.split():\n        counts[word] = counts.get(word, 0) + 1\n    return counts\n\n\nfreq = count_words(text)\nranked = sorted(freq.items(), key=lambda pair: (-pair[1], pair[0]))\nfor word, n in ranked[:5]:\n    print(word, n)\n"
  },
  "js": {
    "caesar": "// Caesar cipher: encode a message then decode it back.\nfunction shiftChar(ch, k) {\n  if (ch >= \"a\" && ch <= \"z\") {\n    const base = \"a\".charCodeAt(0);\n    return String.fromCharCode(((ch.charCodeAt(0) - base + k) % 26) + base);\n  }\n  return ch;\n}\n\nfunction encode(message, k) {\n  let out = \"\";\n  for (const c of message) out += shiftChar(c, k);\n  return out;\n}\n\nconst plain = \"the quick brown fox\";\nconst secret = encode(plain, 3);\nconst back = encode(secret, 23);\nconsole.log(\"plain \", plain);\nconsole.log(\"secret\", secret);\nconsole.log(\"back  \", back);\nconsole.log(\"ok\", back === plain);\n",
    "fizzbuzz": "// FizzBuzz for the numbers 1 through 20.\nfunction classify(n) {\n  if (n % 15 === 0) return \"FizzBuzz\";\n  if (n % 3 === 0) return \"Fizz\";\n  if (n % 5 === 0) return \"Buzz\";\n  return String(n);\n}\n\nconst results = [];\nfor (let n = 1; n <= 20; n++) {\n  results.push(classify(n));\n}\nconsole.log(results.join(\" \"));\n",
    "primes": "// Sieve of Eratosthenes: primes below 50.\nfunction primesBelow(limit) {\n  const sieve = new Array(limit).fill(true);\n  sieve[0] = false;\n  if (limit > 1) sieve[1] = false;\n  for (let i = 2; i < limit; i++) {\n    if (sieve[i]) {\n      for (let j = i * i; j < limit; j += i) sieve[j] = false;\n    }\n  }\n  const out = [];\n  for (let i = 0; i < limit; i++) if (sieve[i]) out.push(i);\n  return out;\n}\n\nconst found = primesBelow(50);\nconsole.log(\"count\", found.length);\nconsole.log(\"primes\", found.join(\",\"));\n",
    "wordcount": "// Count word frequencies in a fixed passage and show the top five.\nconst text = \"the quick brown fox jumps over the lazy dog \"\n  + \"the dog was not amused and the fox ran away \"\n  + \"over the hill and far away the fox went\";\n\nfunction countWords(passage) {\n  const counts = {};\n  for (const word of passage.split(\" \")) {\n    counts[word] = (counts[word] || 0) + 1;\n  }\n  return counts;\n}\n\nconst freq = countWords(text);\nconst ranked = Object.keys(freq).sort((a, b) => freq[b] - freq[a] || (a < b ? -1 : 1));\nfor (const word of ranked.slice(0, 5)) {\n  console.log(word, freq[word]);\n}\n"
  }
};
